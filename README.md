
  # Prototype

  This is a code bundle for Prototype. The original project is available at https://www.figma.com/design/bnsPoNY3hQ5TZdt3w32TCP/Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  